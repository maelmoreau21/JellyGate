# Disable minimum release age restriction for pnpm supply chain policy in Docker & CI
minimum-release-age=0

# Allow puppeteer build scripts (required by pnpm v10+, replaces pnpm.onlyBuiltDependencies in package.json)
onlyBuiltDependencies[]=puppeteer
